import 'package:flutter/material.dart';
class PrivacyPolicyPage extends StatelessWidget{const PrivacyPolicyPage({super.key});@override Widget build(BuildContext context){final c=Theme.of(context).colorScheme;return ListView(padding:const EdgeInsets.all(16),children:[const Text('سياسة الخصوصية',style:TextStyle(fontSize:24,fontWeight:FontWeight.bold)),const SizedBox(height:16),_s('البيانات المستخدمة','يستخدم التطبيق الموقع الجغرافي فقط لحساب مواقيت الصلاة وجدولة الإشعارات.'),_s('التخزين المحلي','يحفظ التطبيق التقدم والمفضلة والوضع الليلي داخل الجهاز فقط.'),Card(color:c.primaryContainer,child:Padding(padding:const EdgeInsets.all(16),child:Text('هذا التطبيق لا يجمع بيانات شخصية ولا يشارك المعلومات مع أي جهة خارجية.',style:TextStyle(fontWeight:FontWeight.bold,color:c.onPrimaryContainer))))]);}Widget _s(String t,String b)=>Card(child:Padding(padding:const EdgeInsets.all(16),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(t,style:const TextStyle(fontSize:18,fontWeight:FontWeight.bold)),const SizedBox(height:8),Text(b,style:const TextStyle(height:1.7))])));}
